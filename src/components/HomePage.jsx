import Button from "@mui/material/Button";
import React from "react";

const HomePage = () => {
  return (
    <div>
      {" "}
      <Button href="/driver-checkin"> Driver Check in </Button>
    </div>
  );
};

export default HomePage;
